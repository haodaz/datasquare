# Talent Web Search - Stage 0: AI Think (预检策略规划) 架构升级

您提出了一个极其深刻且直击痛点的架构洞察！
我们之前的逻辑是典型的“工程师思维”：线性执行，遇到中文就硬转拼音（导致吴恩达变成 Wu EnDa，完全搜不到他的英文论文），然后只能靠后面的环节去“打补丁”。
而您的思路是真正的“Agentic (智能体) 思维”：**在动手发请求之前，先让 AI 大脑转一下，规划好所有的子任务搜索策略。**

本方案旨在重构管线的开头，引入 **Stage 0: AI Think** 环节。

## 业务背景理解
- **痛点**：对于知名华人学者（如吴恩达、李飞飞）或拥有特定英文名的学者，简单的“中文转拼音”会导致 Google Scholar、ORCID 阶段全部脱靶（搜不到 Wu EnDa，只认 Andrew Ng）。而且盲目搜索缺乏领域关键词限制，容易引入同名噪音。
- **解决方案**：在一切搜索开始前，单独调用一次 LLM（调用其自身的庞大世界知识库），先对这个名字（及机构）进行分析，吐出一份精准的**《检索策略书 (Search Strategy)》**。后续所有的工程搜索步骤，全部按照这份策略书去执行。

---

## Proposed Changes

我们将在 `src/lib/tools/talentWebSearch.ts` 的最顶端（`runTalentWebSearchStream` 函数刚开始时）插入 **Stage 0**。

### [MODIFY] src/lib/tools/talentWebSearch.ts

**新增 Stage 0: 预检策略规划 (AI Think / Strategy Planning)**

1. **策略生成 Prompt**
   - **输入**：`searchName`（可能是中文也可能是英文）、`institution`（如果有）。
   - **任务**：利用大模型自身的世界知识，推断这个人的中英文真名、研究领域，并为不同的搜索引擎设计最佳 Query。
   - **Prompt 示例**：
     ```text
     你是一个专业的学术情报检索专家。我需要检索学者 "{searchName}" (机构: {institution})。
     请利用你的知识储备判断他/她最可能的真实中英文姓名、主要研究领域，并为后续的工具生成专属检索词。
     即使你不太确定，也请给出最合理的推测（比如根据常规习惯给出拼音变体）。
     请返回严格的 JSON 格式：
     {
       "thought_process": "你的思考过程，比如吴恩达的英文名是 Andrew Ng...",
       "chinese_name": "吴恩达",
       "english_name": "Andrew Y. Ng",
       "research_fields": ["人工智能", "深度学习"],
       "scholar_queries": ["Andrew Ng", "Andrew Y. Ng"], // 优先用标准英文名
       "orcid_queries": ["Andrew Ng"], // ORCID 最好用的英文名
       "wiki_queries": ["吴恩达", "Andrew Ng"]
     }
     ```

2. **改造后续的搜索阶段 (Stage 1-4)**
   - **废弃硬拼音转换**：移除原本在 Scholar 和 ORCID 阶段使用的、呆板的 `generatePinyinVariants` 逻辑（如果是 AI 规划的，它自然会把拼音变体放在 query 列表里；如果是吴恩达，它就会放 Andrew Ng）。
   - **Stage 2 (Scholar)**：遍历 `strategy.scholar_queries` 执行搜索，而不是死板地查中英文名。
   - **Stage 2.5 (ORCID)**：遍历 `strategy.orcid_queries` 执行搜索。
   - **Stage 3 (Wikipedia/Baike)**：使用 `strategy.wiki_queries` 辅助搜索，并利用 `strategy.research_fields` 过滤同名歧义词条。
   
3. **前端日志展示**
   - 在前端第一行输出：`[预检规划] AI 大脑已激活。判断 {searchName} 英文名为 {english_name}，领域为 {fields}。已生成专属检索策略。`

---

## User Review Required

> [!IMPORTANT]
> **延迟说明**
> 在所有的并发 API 请求开始前，增加这一次 AI 调用会使得“看到第一条搜索结果”的时间**延后 2-3 秒**。但换来的是后续步骤（Scholar、ORCID 等）的命中率呈指数级上升，彻底解决“吴恩达变 Wu EnDa”这种荒谬问题。

## Open Questions

> [!TIP]
> **生僻名字/年轻学者问题**
> 像李飞飞、吴恩达这样的大牛，AI 大脑里肯定有知识。但如果您输入的是一个刚刚毕业、没有任何名气的年轻学者（比如“张三”，某某普通公司），大模型的世界知识里肯定没有他。
> **我的处理策略**是：在 Prompt 里告诉 AI，如果知识库里查无此人，就老老实实退化为“基于拼音规则生成 `english_name`”和“生成常规检索词”。
> 这样可以做到“大牛精准打击，小白兜底转化”。您同意这种 fallback（退化）策略吗？

## Verification Plan

### Manual Verification
1. 重启后在测试工具输入“吴恩达”。
2. 观察日志流的第一步，是否成功进行了 `[预检规划]`，并且准确输出了 `Andrew Ng`。
3. 观察后续的 ORCID 和 Scholar 阶段，是否使用了 `Andrew Ng` 作为 Query 而不是 `Wu EnDa`。
4. 再测试一个生僻的普通中文名字，确认是否能正常生成拼音进行兜底检索。
