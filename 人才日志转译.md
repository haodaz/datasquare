# 人才日志转译 — 三个问题分析与修复

## 问题 1：转译数据全是 null

**根因**：[translate/route.ts](file:///Users/aisandbox/Documents/datasquare/src/app/api/admin/talent-journal/translate/route.ts) 第 20 行还在调 `mcpTools.dashGenericSearch` 从平方数据库读原始数据。但数据已经存在 Supabase 了，读不到 → AI 什么信息都没有 → 输出全 null。

**修复**：改为从 Supabase `talent_entries` 表读取数据。

---

## 问题 2：MCP 二进制报错

**根因**：同上，`mcpTools.dashGenericSearch` 需要 MCP 二进制。

**修复**：解耦后完全不需要 MCP。

---

## 问题 3：实体字段模型

**现状**：当前的 `TalentJournalEntry` 类型中 `work_history`、`education_raw` 都是 `string | null` — 但参考 yida 的 `CRMPeWorkExperiences` 和 `CRMPeEduBackgrounds`，这些应该是**结构化的 list**。

**参考 yida 模型**：
```
CRMPeEduBackgrounds: degree, school_name_cn, major_name_cn, start_date, end_date ...
CRMPeWorkExperiences: employer, position, department, start_date, end_date ...
```

**修复**：在转译时，让 AI 输出结构化的 list，而不是纯文本。

---

## Proposed Fix

#### [MODIFY] [translate/route.ts](file:///Users/aisandbox/Documents/datasquare/src/app/api/admin/talent-journal/translate/route.ts)

1. **删掉** `mcpTools.dashGenericSearch` — 改为从 Supabase 读 `talent_entries` 表
2. **丰富 Prompt** — AI 输出的 JSON 包含结构化子实体（教育/工作经历为 list）
3. **对齐 yida 实体模型** — 字段命名参考 `CRMTalentPerson` + 子实体

新的 AI 输出格式：
```json
{
  "name_cn": "张锋",
  "name_en": "Feng Zhang",
  "orcid_id": "0000-0002-5869-...",
  "h_index": 120,
  "institution": "MIT",
  "position": "Professor",
  "research_fields": ["基因编辑", "CRISPR", "神经科学"],
  "email": "",
  "nationality": "美国",
  "bio_summary": "...",
  "education": [
    { "degree": "博士", "school": "Stanford University", "major": "Chemistry", "start_year": 2004, "end_year": 2009 },
    { "degree": "本科", "school": "Harvard University", "major": "Chemistry & Physics", "start_year": 2000, "end_year": 2004 }
  ],
  "work_history": [
    { "employer": "MIT", "position": "Professor", "department": "Brain and Cognitive Sciences", "start_year": 2011, "is_current": true },
    { "employer": "Broad Institute", "position": "Core Member", "start_year": 2011, "is_current": true }
  ],
  "awards": ["诺贝尔化学奖 (候选)", "Breakthrough Prize"],
  "other_info": "..."
}
```

改动量不大（只改 translate/route.ts 一个文件），是否继续？
