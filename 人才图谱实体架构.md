# 重构人才图谱实体库架构（支持子实体嵌套与专属列表展示）

## 目标与架构设计说明
根据你的点拨，我彻底理解了我们的**数据流转核心逻辑**。这不是一个非此即彼的选择题，而是一个清晰的漏斗和组装过程：
1. **源数据层 (Retrieval)**：各种外部搜寻工具（如 Deep Talent Search）带回来的原始信息。那个 Excel 定义的是这一层“搜回来的泥沙”大概长什么样。
2. **转译层 (AI Translation)**：专注做一件事，把搜回来的泥沙（原始 JSON/文本）清洗、提纯，并严格按照我们的**目标人才图谱结构**进行映射。
3. **图谱实体层 (Talent Graph)**：这是我们最终的结构化数据库，高度定制化、可视化。包含嵌套的“教育背景”、“工作经历”、“奖项”、“专利”、“论文”等子实体。

我们将重构 AI 转译逻辑与实体库存储，固化这个**人才图谱结构**。转译后的结果将是一个标准的多维度 JSON，完美适配详情页的多表格卡片展示。

## Open Questions
- **存储方案确认**：为了保证图谱的灵活性，我计划在 `talent_db_entities` 表中直接新增 5 个 `JSONB` 类型的列（`educations`, `work_experiences`, `awards`, `patents`, `papers`）来存储提纯后的子实体数组。是否同意采用 `JSONB` 存储方案？
- **字段融合**：在转译环节，AI 会把源数据中扁平的本科/硕士/博士信息，统一提纯重组为一个标准的 `educations` 对象数组（包含学位、学校、专业、起止时间等），不再保留扁平结构。可以吗？

## Proposed Changes

### 1. 数据库结构修改 (SQL / `talent_db_entities`)
通过 SQL 给 `talent_db_entities` 增加 5 个 JSONB 字段来存储子实体：
- `educations` (JSONB)
- `work_experiences` (JSONB)
- `awards` (JSONB)
- `patents` (JSONB)
- `papers` (JSONB)

### 2. AI 转译 Prompt 重构 (`/api/admin/talent-journal/translate/route.ts`)
修改 Prompt，要求 AI 在返回的 JSON 中，不仅包含基本的平面字段，还必须输出结构化的数组。
例如：
```json
{
  "educations": [
    { "degree": "博士", "school": "斯坦福大学", "major": "计算机科学", "start_time": "2015", "end_time": "2019" }
  ],
  "work_experiences": [
    { "company": "OpenAI", "department": "AI 研究院", "position": "研究员", "start_time": "2020", "end_time": "至今" }
  ],
  "awards": [
    { "time": "2023", "name": "图灵奖" }
  ]
}
```

### 3. 数据导入逻辑更新 (`/api/admin/talent-db/import/route.ts`)
更新映射逻辑，将提取出的新数组结构直接 `insert` 或 `update` 到对应的 `JSONB` 列中。

### 4. 前端详情页 UI 重构 (`/admin/talent-db/[id]/page.tsx`)
参考你给出的“平方创想”截图，彻底重构详情页 UI。
- 顶部保留基本信息区块（姓名、工作、标签等）。
- 下方按模块渲染多个卡片，每个卡片对应一个子实体。
- 卡片内部使用 Ant Design 的 `Table` 组件进行展示，并预留“添加 / 导出”等操作按钮的基础样式。

## Verification Plan
1. 执行 SQL 添加 `JSONB` 列。
2. 挑选一条数据重新点击“转译”，检查控制台返回的新 JSON 结构是否为标准的对象数组。
3. 点击“导入人才库”，前往人才实体库详情页，验证是否成功渲染了多个子实体表格。
